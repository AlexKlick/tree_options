"""tree_options: point-in-time equity signal research through options.

M0 scope: protocol and invariant harness. Nothing in this package trains
models, ingests vendor data, or produces backtest returns.
"""

__version__ = "0.1.0"
