// Phase 3 mount point: the options surface replaces this stub wholesale —
// SymbolPage only owns placing it in the Options tab.

export function OptionsPanelStub() {
  return (
    <div className="card empty-state">
      <p className="muted">Options surface lands in the next change</p>
    </div>
  )
}
